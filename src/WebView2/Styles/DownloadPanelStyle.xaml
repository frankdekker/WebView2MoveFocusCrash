<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="Download.Panel.Border" TargetType="Border">
        <Setter Property="BorderThickness" Value="0 1 0 0"/>
        <Setter Property="BorderBrush" Value="#C5C7C8"/>
        <Setter Property="Background" Value="#F0F0F0"/>
    </Style>
    <Style x:Key="Download.Panel.Close.Border" TargetType="Border">
        <Setter Property="CornerRadius" Value="5" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="#DBDDDD" />
        <Setter Property="Background" Value="#efefef" />
        <Setter Property="Margin" Value="10" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="Height" Value="30" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="true">
                <Setter Property="BorderBrush" Value="#C5C7C8" />
                <Setter Property="Background" Value="#dddddd" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="Download.Panel.Close.Cross" TargetType="Line">
        <Setter Property="StrokeThickness" Value="1" />
        <Setter Property="Stroke" Value="#444444" />
    </Style>
</ResourceDictionary>